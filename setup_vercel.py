import os

vercel_json_content = """{
  "version": 2,
  "builds": [
    {
      "src": "backend/main.py",
      "use": "@vercel/python"
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "frontend/$1"
    }
  ]
}
"""

with open("vercel.json", "w", encoding="utf-8") as f:
    f.write(vercel_json_content)

print("[SUCCESS] vercel.json created for dual React + Python Vercel deployment!")